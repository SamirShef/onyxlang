fun main(argc: i32, argv: **char): i32 { // default cli arguments
    var i: i32 = 16;
    var a: *i32 = &i;
    var b: **i32 = &a;
    i = add(a, i);
    echo **b;
    echo '\n';
    return 0;
}

fun add(a: *i32, b: i32): i32 {
    *a += b;
    return *a;
}
